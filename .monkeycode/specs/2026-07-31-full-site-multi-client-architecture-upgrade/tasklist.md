# 需求实施计划

- [x] 1. 建立波次 0 全站资产与契约基线
  - [x] 1.1 实现全站功能资产扫描器
    - 新增 `real_sync/scripts/platform_inventory.mjs`，扫描页面、PHP API、Worker、Cron、PWA、小程序、迁移、文件和 AI 消费者，并输出稳定子项 ID。
    - 将扫描结果关联 `function-matrix.md` 中 89 个组级功能 ID，输出生命周期、入口、路径、消费者和风险字段。
    - 将工作量与招聘并行改动路径标记为冻结所有权，架构升级只通过新增 Kernel 或 Adapter 接入。
    - 需求引用：需求 1.1-1.5、2.1-2.5；设计 8、13 波次 0。
  - [x] 1.2 实现基线清单一致性测试
    - 新增 `real_sync/scripts/platform_inventory.test.mjs`，验证 ID 唯一、路径存在、组级覆盖、生命周期枚举和冻结边界。
    - 验证 API、页面、Worker、Cron、PWA、小程序和 AI 消费者均进入清单。
    - 需求引用：需求 1.1-1.5；设计正确性属性 6。
  - [x] 1.3 固化 API 和客户端契约快照
    - 扩展现有快照方式，记录统一响应、认证、权限、状态版本、幂等键、请求 ID 和主要消费者。
    - 为历史入口保存兼容基线，支持后续迁移漂移检查。
    - 需求引用：需求 2.4、4.1-4.6；设计 3.2、4。
  - [x] 1.4 建立波次 0 自动门禁命令
    - 新增聚合检查脚本，串联资产清单、API 契约、迁移状态、PWA、小程序路由和冻结路径检查。
    - 输出机器可读状态和失败项，保持命令只读与可重复执行。
    - 需求引用：需求 1.5、13.1-13.2；设计 12、13 波次 0。

- [x] 2. 实现统一 API Kernel 与请求上下文
  - [x] 2.1 创建 API Kernel 核心类型
    - 新增 `real_sync/api/kernel/ApiException.php`、`ApiResponse.php`、`RequestContext.php` 和 `bootstrap.php`。
    - 统一 HTTP 状态、业务码、消息、数据、请求 ID、客户端、版本和业务域字段。
    - 需求引用：需求 3.1-3.5、4.6、11.1-11.2；设计 3.2。
  - [x] 2.2 实现请求 ID、异常映射和脱敏日志
    - 接收合法请求 ID或生成新 ID，并传播到响应、结构化日志和调用上下文。
    - 对手机号、Token、OpenID、简历、录音和供应商响应执行字段级脱敏或摘要化。
    - 需求引用：需求 8.2、8.6、11.1-11.4；设计 3.2、7.4。
  - [x] 2.3 实现统一身份与权限适配器
    - 复用 `api/common/context.php` 的员工、角色、门店、岗位、任职和会话版本，形成 Kernel `AuthContext`。
    - 为 Admin 具名权限和数据范围提供兼容适配层，保留现有入口行为。
    - 需求引用：需求 3.1-3.5；设计 3.2、3.3。
  - [x] 2.4 实现 API Kernel 契约和属性测试
    - 覆盖成功、验证错误、未认证、权限拒绝、冲突、内部错误和请求 ID 传播。
    - 属性 2：所有客户端可见数据均属于服务端授权范围。
    - 属性 3：业务对象成功状态变化后版本单调递增。
    - 需求引用：需求 3、4、11；设计正确性属性 2、3。
  - [x] 2.5 迁移首批低风险只读端点
    - 选择健康、能力版本和只读状态端点接入 Kernel，验证兼容门面模式。
    - 保持历史响应字段，增加稳定 `request_id` 和版本元数据。
    - 需求引用：需求 4.5-4.6；设计 3.2、11。

- [x] 3. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
  - 当前 Kernel、企业微信与波次 0 回归通过；全量 793 项中 4 项并行基线测试待对应所有者收口。
  - 后续波次已收口上述 4 项并行基线；最终全量 Node 回归 983/983、PHP 语法 465/465 通过。

- [x] 4. 实现认证、会话与多端同步公共契约
  - [x] 4.1 实现版本化会话与刷新服务
    - 支持短期访问令牌、刷新轮换、会话族、会话版本和刷新令牌复用撤销。
    - PWA 刷新 Cookie 使用 `Secure`、`HttpOnly`、`SameSite=Lax` 和受限 `Path`。
    - 已完成服务端会话核心、PDO 行锁轮换、令牌摘要存储、复用安全事件和访问令牌即时会话状态校验；PWA Cookie 与请求安全接入在 4.2 完成。
    - 需求引用：需求 3.3、12.1-12.2、12.7-12.10；设计 5.4。
  - [x] 4.2 实现 PWA 刷新安全和多标签协调
    - 校验 Origin、CSRF 请求头、会话版本和轮换状态。
    - 多标签页只广播会话版本与刷新协调事件，禁止传播 Token 值。
    - 已完成版本化 PWA 登录、受限刷新 Cookie、签名双提交 CSRF、Web Locks 串行刷新、内存访问令牌与退出撤销。
    - 需求引用：需求 12.7-12.10；设计 5.4。
  - [x] 4.3 实现小程序短期设备会话适配
    - 将会话绑定员工、微信身份、客户端和会话版本，统一 401 刷新队列和重新认证状态。
    - 保持现有小程序 Token 调用兼容，提供能力版本响应。
    - 已完成微信/企微身份摘要与设备绑定、刷新令牌轮换、退出撤销、单飞 401 刷新、上传重试、统一重新认证状态和能力版本 `1.1.0`；定向测试 24/24 通过，全量 800 项中 4 项既有并行基线失败。
    - 需求引用：需求 6.1-6.5、12.11；设计 6。
  - [x] 4.4 实现多端同步等级与冲突协议
    - 为等级 A、B、C 提供状态版本、ETag、增量游标、墓碑状态和后台恢复刷新契约。
    - 实现服务端草稿版本、基础版本校验和 HTTP 409 恢复数据。
    - 已完成同步等级元数据、确定性 ETag、授权范围绑定签名游标、稳定分页、删除与权限失效墓碑、24 小时字段白名单草稿、草稿乐观锁和权威状态 409 恢复数据；定向测试 20/20 通过，全量 804 项中 4 项既有并行基线失败。
    - 需求引用：需求 4.1-4.12；设计 4。
  - [x] 4.5 编写会话与同步正确性测试
    - 覆盖刷新轮换、令牌复用、账号停用、权限撤销、并发刷新、跨标签协调和设备会话。
    - 属性 1：同一用户、动作和幂等键只产生一个业务结果。
    - 属性 4：多端读取同一稳定 ID 与版本时获得相同状态。
    - 已完成 PWA 双标签刷新串行化、跨标签撤销、延迟并发 401 单次刷新、小程序设备会话、会话轮换与复用撤销、账号状态失效、固定种子幂等属性和多端状态一致性属性测试；定向测试 27/27 通过，全量 808 项中 4 项既有并行基线失败。
    - 需求引用：需求 3、4、12；设计正确性属性 1、4。

- [x] 5. 实现数据库演进与双版本回滚门禁
  - [x] 5.1 扩展迁移清单和 readiness 检查
    - 为迁移增加表、列、索引、校验和、数据核对与兼容版本声明。
    - API 启动只执行结构就绪检查，结构变更统一进入版本化迁移。
    - 已新增独立迁移 catalog、固定 SQL 校验和、数据核对分类、待验证 N/N-1 兼容要求和纯查询 readiness；会话刷新、小程序设备会话与同步端点在结构漂移时返回稳定 503，CLI readiness 额外执行部署前数据核对。迁移定向测试 14/14、平台回归 24/24 通过；全量 813 项中 4 项既有并行基线失败。
    - 需求引用：需求 7.1-7.6；设计 9、9.1。
  - [x] 5.2 实现 expand-migrate-contract 验证器
    - 校验兼容窗口内无字段删除或重命名、新增字段具备保留数据的默认值或写入适配器。
    - 校验版本 N 与 N-1 的读写契约、状态降级映射和功能开关门禁。
    - 已新增纯静态兼容验证器，阻断字段删除与重命名，校验新增字段默认值、可空语义或 N/N-1 写入适配器，并验证状态降级映射和兼容窗口功能开关；验证器接入 apply、readiness 与机器可读 compatibility 命令。迁移定向测试 18/18 通过；全量 817 项中 4 项既有并行基线失败。
    - 需求引用：需求 7.7-7.9、13.15-13.18；设计 9.1、14。
  - [x] 5.3 实现迁移重放与核对工具
    - 基于变更日志和 outbox 核对回滚后的业务写入、异步事件和外部副作用。
    - 输出 dry-run、verify 和 rollback-plan 的机器可读证据。
    - 已新增只读有界证据采集器、稳定证据摘要、差异核对与保留式重放计划，支持数据库时间窗和 stdin 固定证据；迁移定向测试 24/24 通过，全量 823 项中 4 项既有并行基线失败。
    - 需求引用：需求 7.8-7.9、13.15-13.18；设计 9.1、14。
  - [x] 5.4 迁移非工作量运行时 DDL
    - 盘点并迁移 Admin、企微、提醒、技能和专题模块中的请求期建表逻辑。
    - 工作量和招聘目录保持冻结，待并行任务稳定后独立收口。
    - 已新增身份审计、企微投递、提醒投递、技能复盘和专题业务五个增量迁移，将非冻结模块的请求期建表与提醒规则种子数据迁入迁移 CLI；历史入口仅保留按依赖版本执行的只读 readiness，工作量与招聘目录保持冻结。迁移与提醒定向测试 24/24、expand-migrate-contract 门禁通过；全量 827 项中 4 项既有并行基线失败。
    - 需求引用：需求 7.2-7.3；设计 13 波次 2。
  - [x] 5.5 编写迁移兼容与属性测试
    - 覆盖重复执行、结构漂移、无效引用、双版本读写、回填、状态降级和副作用核对。
    - 属性 12：兼容窗口内新数据可由 N 与 N-1 确定性解释。
    - 属性 13：N 与 N-1 写入均保留当前结构全部业务事实。
    - 已新增 `scripts/migration_compatibility.property.test.mjs`，对全部 34 个迁移执行固定种子的 N/N-1 兼容投影和写入守恒属性测试，并联结重复执行、结构漂移、数据核对、历史回填、无效引用、状态降级和副作用核对契约。迁移定向回归 34/34、兼容门禁通过；全量 831 项中 4 项既有并行基线失败。
    - 需求引用：需求 7、13.15-13.18；设计正确性属性 12、13。

- [x] 6. 实现平台健康、SLI 与发布门禁
  - [x] 6.1 创建分层健康端点
    - 新增 live、ready 和 dependencies 健康端点，覆盖应用、数据库、迁移版本、队列、Worker 和外部依赖。
    - 健康响应使用 Kernel 契约并限制敏感配置暴露。
    - 已新增 `api/platform/HealthService.php` 与 `api/platform/health.php`，支持 `check=live|ready|dependencies`；live 零依赖，ready 校验数据库与 34 个迁移，dependencies 汇总数据库、迁移、队列、Worker 和外部配置状态，异常只返回稳定错误分类。PHP 语法检查与平台健康定向回归 12/12 通过。
    - 需求引用：需求 10.1-10.9、11.5；设计 7.4、12.3。
  - [x] 6.2 实现 Tier-1 合成旅程与 SLI 聚合
    - 对官网、合成登录、员工入口和核心 API 每分钟执行至少两次探测。
    - 按自然月总分钟计算可用率，计划维护计入分母，并区分超时、5xx、结构和权威状态失败。
    - 已新增 `scripts/platform_sli.mjs`，单次运行对四条 Tier-1 旅程各执行两次探测并输出脱敏 JSON；聚合器要求每条旅程每分钟至少两次成功，按自然月总分钟计算 99.9% 目标，计划维护继续计入分母。定向测试 7/7 通过。
    - 需求引用：需求 10.1、10.7、10.10-10.12；设计 7.4。
  - [x] 6.3 实现发布观察与停止判定器
    - 实现 15、30、60 分钟观察窗口和旅程失败、5xx、P95、数据差异、任务积压、任务失败及权限拒绝阈值。
    - 保存批次范围、备份、验证、观察、决策和恢复证据。
    - 已新增 `scripts/platform_release_gate.mjs`，按批次类型执行 15/30/60 分钟观察门禁，精确实现七类停止条件，并生成包含范围、备份、验证、观察、决策和恢复字段的稳定证据。稳定性定向回归 11/11 通过。
    - 需求引用：需求 13.1-13.14；设计 14。
  - [x] 6.4 编写 SLI 与发布阈值测试
    - 覆盖最低样本、窗口边界、连续失败、计划维护、低流量和多指标同时越界。
    - 验证同一输入始终形成唯一可用率和发布决策。
    - 已新增 `scripts/platform_release_gate.property.test.mjs`，覆盖最低样本、15/30/60 分钟精确边界、成功探测重置连续失败、计划维护自然月分母、低流量样本门禁和多指标并发判定；128 组固定种子输入验证月度可用率与发布决策确定性。定向回归 16/16 通过，全量 Node 回归 847 项中 843 通过，失败仍为 4 项既有冻结基线。
    - 需求引用：需求 10、13；设计 7.4、14。

- [x] 7. 实现统一 Worker、Cron 和异步副作用治理
  - [x] 7.1 创建任务租约与 fencing 公共组件
    - 新增 JobLease、JobRunner 和 RetryPolicy，统一 Worker ID、租约、心跳、fencing token、有限重试和 dead-letter 状态。
    - 迟到 Worker 在租约失效后无法提交结果。
    - 已新增 `PlatformJobLease`、`PlatformJobRunner`、`PlatformPdoJobStore` 与 `PlatformRetryPolicy`，领取事务递增 fencing token 并记录运行摘要，心跳、完成、重试和 dead-letter 均校验 Worker、token 与未过期租约；最终尝试租约过期会自动收敛为待人工恢复的 dead-letter。迁移 `202607310010_platform_jobs.sql` 以幂等任务身份、领取/积压索引和逐次运行记录承载公共契约。定向回归 16/16 通过；全量 Node 回归 851 项中 847 通过，失败仍为 4 项既有冻结基线。
    - 需求引用：需求 9.3-9.8；设计 7.3。
  - [x] 7.2 实现 transactional outbox 与副作用幂等
    - 在业务事务中保存待投递事件，消息、AI、文件和业务写入使用副作用幂等键。
    - 提供人工重放、失败分类和补偿状态。
    - 已新增 `PlatformOutboxService` 与迁移 `202607310011_platform_outbox.sql`。业务写入必须在调用方 PDO 事务内保存事件；事件和副作用分别按稳定幂等键与规范载荷 SHA-256 去重。副作用开始、确认、失败和补偿提交同时校验有效任务租约，并在持久化更新中绑定任务、Worker 与 fencing token；确认结果只写入一次，事件随消费进入 `processing`、`dispatched`、`failed` 或 `recovery_required`。人工重放保留事件键、载荷和摘要并记录操作人、原因与次数；补偿使用独立状态保留原确认结果。定向回归 19/19 通过；全量 Node 回归 854 项中 850 通过，失败仍为 4 项既有冻结基线。
    - 需求引用：需求 7.9、9.7-9.9；设计 7.3、9.1。
  - [x] 7.3 迁移首批非冻结 Worker 与 Cron
    - 优先迁移提醒、企微、技能和治理任务，消除生产绝对路径并记录运行摘要。
    - 招聘和工作量 Worker 通过 Adapter 保持现状，待并行改动结束后迁移。
    - 已新增统一 `PlatformJobQueue`、`PlatformJobDispatcher`、CLI dispatcher 和四类 Handler registry；提醒、企微、技能复盘与演练治理写路径均通过事务 Adapter 入队，dry-run 治理保持同步预览。技能上传在同一事务保存业务记录和平台任务，旧 Cron 补捞历史待处理记录，录音与 SKILL 路径改为部署根目录相对解析。企微同步对空成员响应执行保护性失败，dispatcher 在租约丢失时立即终止，健康检查公开最老待处理年龄并在 300 秒触发 degraded。迁移 `202607310012_platform_job_dispatch.sql` 增加规范载荷摘要；定向回归 23/23 通过，全量 Node 回归 861 项中 857 通过，失败仍为 4 项既有冻结基线。
    - 需求引用：需求 9.3-9.9、11.1-11.2；设计 13 波次 2、4。
  - [x] 7.4 编写任务正确性与恢复测试
    - 覆盖租约竞争、心跳、fencing、重复副作用、指数退避、dead-letter、人工重放和进程中断。
    - 属性 9：只有最新 fencing token 持有者可以提交结果。
    - 属性 10：同一副作用幂等键只形成一次已确认结果。
    - 已新增 `scripts/platform_job_recovery.property.test.mjs`。属性 9 以 128 组固定种子、每组 256 次租约领取、时间推进、心跳、失败和提交操作验证重领后仅最新 fencing token 可提交；属性 10 以相同规模随机模拟 Worker 中断与副作用恢复，验证稳定幂等键只形成一次外部执行和一致确认结果。确定性恢复序列覆盖进程中断、旧 Worker 迟到、租约重领、5/10/20 秒有上限指数退避、最终 dead-letter 和保留事件身份与载荷的人工重放；源码契约同步约束生产任务与副作用持久化条件。任务层联合回归 18/18、健康与迁移回归 6/6、核心 PHP 语法通过；全量 Node 回归 865 项中 861 通过，失败仍为 4 项既有冻结基线。
    - 需求引用：需求 9；设计正确性属性 9、10。

- [x] 8. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
  - 已同步过时的员工权限、员工工号唯一索引和工作量预警管理页面契约；预警页面使用服务端结构化 `evidence` 展示事实证据，并以 `event_id`、`comment` 提交处理请求。工作量页面定向回归 9/9、全量 Node 回归 866/866 通过。

- [x] 9. 完善手机与桌面 PWA
  - [x] 9.1 统一 `/mobile/` PWA 应用入口
    - 保持 Manifest `start_url` 和 PWA 内部导航位于 `/mobile/`，将 `/internal.html` 固化为浏览器兼容启动与受控跳转入口。
    - 完善手机、平板和桌面导航与布局边界。
    - 需求引用：需求 5.1-5.3、5.8；设计 5。
    - 已新增 `/mobile/index.html` 与同源白名单目标解析器，Manifest、登录默认路径和 `/internal.html` 认证恢复统一进入 `/mobile/`；工作量、演练和我的共享 768/1024 像素响应式应用壳。PWA 定向回归 15/15、平台预检与资产测试 7/7、全量 Node 回归 869/869 通过。
  - [x] 9.2 实现统一 PWA ApiClient
    - 支持超时、请求 ID、错误映射、刷新协调、幂等键、状态版本、ETag、增量游标和 409 冲突恢复。
    - 将业务页面分批迁移到统一客户端，工作量页面在并行开发完成后接入。
    - 需求引用：需求 4、5.5、12.7-12.10；设计 4、5。
    - `js/api-client.js` 已统一委托 `AppAuth.authFetch()` 协调刷新，并提供 15 秒默认超时、`X-Request-ID`、稳定错误分类、显式幂等键、可配置状态版本字段、内存 ETag、增量游标及携带权威状态的 409 单次恢复回调。演练页面已接入幂等与冲突后权威状态刷新，工作量和管理页面保持既有响应契约并升级客户端版本。ApiClient 单元测试 5/5、定向兼容回归 32/32、平台预检与 PWA 资产测试 9/9、全量 Node 回归 874/874 通过。
  - [x] 9.3 完善 Service Worker 和版本更新
    - 缓存应用外壳与批准公共资源，排除 API、私有文件和敏感业务数据。
    - 实现等待更新、用户确认、旧缓存清理、离线壳和登录恢复。
    - 需求引用：需求 5.4-5.7；设计 5.3。
    - Service Worker v4 已使用显式路径白名单缓存应用壳，API、管理端、上传、私有文件与未批准页面均绕过共享缓存；批准页面采用网络优先恢复，公共静态资源采用缓存优先，离线导航进入专用只读离线壳。新 Worker 安装后保持 waiting，Runtime 查询并展示版本号，用户确认后发送 `SKIP_WAITING`，激活时只清理旧 PWA 缓存并恢复原页面。更新和网络恢复后通过 `AppAuth.ensureAccessToken()` 重新校验会话，并发布恢复事件供业务页面刷新状态。Service Worker 行为与 PWA 联合回归 17/17、平台预检通过、全量 Node 回归 880/880 通过。
  - [x] 9.4 实现受控草稿与恢复
    - 草稿按用户、业务域、对象和版本隔离，仅保存批准字段并使用不超过 24 小时有效期。
    - 网络恢复、权限变化、退出和会话撤销时执行版本核对与敏感草稿清理。
    - 需求引用：需求 4.11、5.4-5.5、12.2-12.4；设计 4、5.3、5.4。
    - 已新增统一 `DraftStore`，本地键按用户、员工、业务域、对象和 schema 版本隔离，保存时裁剪批准字段并将有效期限制在 24 小时；工作量 H5 已接入本地即时恢复、服务端草稿乐观锁、设备标识、网络与会话恢复核对及跨设备显式版本选择。退出、会话撤销和会话版本变化会发布敏感清理事件，Service Worker v5 将草稿运行时加入批准应用壳。草稿与 PWA 定向回归 29/29、平台预检通过、全量 Node 回归 886/886 通过。
  - [x] 9.5 完善桌面可访问性与响应式测试
    - 覆盖小于 768、768 至 1023、1024 及以上、横竖屏、200% 缩放、键盘焦点和 Escape 关闭。
    - 覆盖安装、独立窗口、离线、更新、缓存边界和会话恢复。
    - 需求引用：需求 5.1-5.10；设计 5、12.2。
    - 四个核心业务页已统一共享小于 768、768 至 1023、1024 及以上三档应用壳，补齐横屏低高度回流与全站移动页 200% 缩放能力；学习中心主要操作、我的菜单和课程分类已使用原生键盘语义，修改密码、演练 Sheet 与工作量凭证预览已具备模态语义、初始焦点、焦点循环、Escape 关闭和焦点恢复。新增 PWA 无障碍契约测试 7/7，PWA 定向回归 26/26、平台预检通过、全量 Node 回归 893/893 通过；真实浏览器 200% 缩放保留为发布前人工验收项。

- [x] 10. 固化小程序接入契约和现网保活
  - [x] 10.1 统一小程序请求与上传层
    - 统一请求 ID、错误、超时、刷新队列、幂等键、状态版本、上传摘要和失败恢复。
    - 收敛直接 `wx.request` 与 `wx.uploadFile` 调用，保留现网页面行为。
    - 需求引用：需求 6.1-6.5；设计 6。
    - 小程序业务页面已统一通过 `utils/api.js` 访问网络，公共层为请求和上传传播请求 ID、幂等键与状态版本，普通请求默认 15 秒、上传默认 60 秒，并提供稳定错误分类、冲突恢复元数据、单飞刷新与一次重放。登录和绑定使用显式匿名请求；上传通过微信文件信息接口计算 SHA-256 并附加到表单，网络、超时和协议错误提供可重试状态。新增小程序 API 客户端契约测试 7/7，定向回归 31/31、平台预检通过、全量 Node 回归 900/900 通过。
  - [x] 10.2 修复现网阻断导航与能力版本
    - 修正 Tab 与普通导航语义、登录阻断和请求层阻断问题。
    - 增加能力版本响应，为旧版小程序提供受控功能展示。
    - 需求引用：需求 6.4-6.5；设计 6 小程序阶段边界。
    - 新增 `utils/navigation.js`，按 `app.json` 的 Tab 清单统一普通页、Tab 页、替换与重启导航，并通过暂存查询参数支持 Tab 消息入口；登录后普通首页改用 `reLaunch`。受保护请求在本地无 Token 时直接进入受控重新认证且不发送网络请求，提醒授权页增加“稍后设置”通路。公开能力端点升级到 `1.3.0`，发布显式功能白名单和最低客户端版本，首页在端点异常或字段缺失时只显示核心能力。新增运行时守卫契约测试 5/5，相关定向回归 20/20、平台预检通过、全量 Node 回归 906/906 通过。
  - [x] 10.3 编写小程序静态契约测试
    - 覆盖页面注册、导航、请求层、设备会话、状态同步、上传和能力版本。
    - 保持完整新页面开发、真机验收和灰度发布在后续小程序波次执行。
    - 需求引用：需求 6、13.4；设计 6、12.2。
    - 新增聚合静态契约检查器，覆盖页面注册、导航与 Tab 清单、统一请求层、设备会话、状态版本与冲突恢复、统一上传及能力版本七类契约，并作为 `mini_program_contracts` 阻断检查接入平台预检。新增正向基线和 Tab 漂移负向测试 2/2，相关定向回归 33/33、平台预检通过、全量 Node 回归 908/908 通过；真机验收和灰度发布继续作为后续独立发布条件。

- [x] 11. 实现受控文件服务与统一 AI 能力层
  - [x] 11.1 创建文件资产与访问契约
    - 统一用途、所有者、ACL、MIME、大小、SHA-256、存储位置、留存、下载有效期和审计字段。
    - 为公开静态资产、受控文件、临时导出和敏感原始文件建立分类策略。
    - 需求引用：需求 9.1-9.2、12.5；设计 7.2。
    - 新增 `PlatformFileAssetService`、PDO Store 和 `202607310013_platform_file_assets.sql`，统一文件身份、业务用途、所有者、业务对象、实际 MIME、大小、SHA-256、存储驱动与相对键、访问模式、留存策略、下载有效期和创建审计。ACL 按主体、权限、可选业务范围、有效期和撤销状态保存；访问判定覆盖公开读取、所有者、作用域授权、留存和下载到期，并为允许与拒绝结果写入最小审计事件。四类策略分别约束公开与私有存储驱动、100MB/500MB 大小上限、留存和临时下载期限。新增文件契约测试 3/3、迁移就绪与兼容测试 12/12，PHP 语法、平台预检及全量 Node 回归 911/911 通过；真实 MySQL 迁移和消费者接入保留给 11.2 及隔离数据库验收。
  - [x] 11.2 实现上传、私有下载与过期清理组件
    - 统一实际类型校验、路径规范化、随机存储键、鉴权流式下载和到期清理。
    - 通过 Adapter 接入演练音频；招聘简历和工作量文件在并行改动稳定后接入。
    - 需求引用：需求 9.1-9.2、11.3、12.5；设计 7.2。
    - 新增 `PlatformPrivateFileStorage`，统一 PHP 上传状态、Fileinfo 实际 MIME、声明类型、大小、SHA-256、私有根目录、192 位随机相对键、`0700/0600` 权限、规范化路径、多对象流式响应及幂等留存清理。新增 `DrillMediaStorageAdapter` 将演练资产标记和音频分片迁入 Web 根目录外私有存储；访问接口返回兼容 `url` 的受控下载地址并排除物理键，流式请求再次校验身份、对象、角色范围、真实录音授权与留存期限，同时保存允许或拒绝审计。演练治理 Worker 通过同一 Adapter 执行物理清理并保留领域元数据、评分、复核和认证事实。新增私有文件测试 4/4，文件与 Drill 集成回归 38/38，PHP 语法、差异格式、平台预检和全量 Node 回归 916/916 通过；真实 HTTP、数据库与生产 Worker 验收保留给隔离环境发布门禁。
  - [x] 11.3 创建统一 AI 能力契约
    - 实现 `text.generate`、`assessment.score`、`vision.extract`、`ocr.extract` 和 `speech.transcribe` 的请求、错误、超时、重试、审批和调用摘要契约。
    - 将 `image.generate` 保留为架构扩展点，本轮不接入供应商。
    - 需求引用：需求 8.1-8.7；设计 7.1。
    - 新增 `PlatformAiCapabilityGateway`、可注入供应商执行器与审批决策、稳定 `PlatformAiException` 分类、有限总超时与最多五次尝试、已审批 fallback、调用结果处理版本和 `PlatformPdoAiInvocationStore`。敏感分类默认要求显式审批，审批拒绝或审批服务异常保持零供应商调用；`image.generate` 固定为零调用扩展点。输入、输出和供应商错误只以 SHA-256、字节数或脱敏摘要写入 `platform_ai_invocations`，新增迁移 `202607310014` 并将 catalog 基线提升至 39。AI 契约测试 6/6、迁移治理 12/12、PHP 语法、补丁格式和平台预检通过，全量 Node 回归 922/922 通过；现有消费者入口和业务状态保持到 11.4 再迁移，真实供应商与隔离数据库验收保留给发布门禁。
  - [x] 11.4 收口 AI Runtime 双入口
    - 以 `api/ai-runtime.php` 为权威实现，将根目录副本调整为薄兼容入口。
    - 优先迁移体测和 Drill v2，招聘消费者在并行任务完成后接入。
    - 需求引用：需求 8.1-8.6；设计 7.1、13 波次 4。
    - `api/ai-runtime.php` 已装配 `PlatformAiCapabilityGateway`、DeepSeek `text.generate` 和百度 `ocr.extract` 生产路由，调用摘要优先写入统一数据库表，数据库审计暂不可用时只向服务日志输出脱敏治理元数据。根目录 `ai-runtime.php` 已缩减为保留直接访问 403 的薄兼容入口。体测图片识别与字段提取固定为百度 OCR 加本地确定性解析，移除 DeepSeek 结构化识图和豆包视觉兜底；体测解读、运动规划、夏令营报告及 Drill v2 文本生成通过 DeepSeek 统一能力执行，并由认证业务入口显式提交授权上下文。招聘冻结目录保持未接入。新增双入口与消费者收口测试 5/5，相关 AI、体测与 Drill 定向回归 24/24，PHP 语法、补丁格式和平台预检通过，全量 Node 回归 927/927 通过；真实供应商、数据库审计表和生产 HTTP 验收保留给隔离发布门禁。
  - [x] 11.5 编写文件与 AI 契约测试
    - 覆盖类型伪装、路径边界、权限、下载到期、留存、供应商超时、故障兜底、审批门禁和日志脱敏。
    - 属性 7：敏感文件物理读取经过身份、权限、范围和路径校验。
    - 属性 8：AI 结果关联能力类型、处理版本和可审计状态。
    - 需求引用：需求 8、9、11、12；设计正确性属性 7、8。
    - 扩展私有存储契约测试，覆盖绝对路径、反斜杠、重复分隔符、点段、控制字符、缺失对象和符号链接越界，并验证目录 `0700`、文件 `0600`、多分片全量预解析、精确到期和重复清理幂等。Drill 属性 7 测试通过恶意分片键证明匿名身份、角色范围、对象权限和留存拒绝均先于物理路径解析，合法所有者通过治理校验后才进入私有根路径边界。AI 属性 8 测试遍历五类已启用能力，逐项验证成功结果与唯一调用摘要共享请求 ID、能力、契约版本、处理版本、供应商和完成状态，并验证审批回调收到用途、数据分类、留存策略及权限范围上下文。任务 11.5 定向回归 41/41、平台契约回归 98/98、全量 Node 回归 930/930 通过，补丁格式检查无异常；真实 HTTP、MySQL 和供应商超时联调继续作为隔离发布门禁。

- [x] 12. 按功能矩阵收口业务域与历史入口
  - [x] 12.1 迁移身份、组织、学习、知识、考试和制度域
    - 按稳定功能 ID 将端点接入 Kernel、统一会话、权限、错误、状态版本和审计。
    - 记录历史入口消费者并通过兼容控制器转交稳定服务。
    - 需求引用：需求 1-4、11；设计 3.3、13 波次 5。
    - 新增 `PlatformBusinessDomainRegistry`，登记 `IAM-001`、`IAM-004`、`IAM-009`、`BIZ-014`、`BIZ-015`、`BIZ-016`、`BIZ-018` 和 `MSG-004` 的代表端点、版本、历史消费者与能力。身份、组织、学习、知识、考试和制度六个原 URL 已迁移为 Kernel 兼容控制器，并分别委托身份上下文、组织树、课时进度、知识列表、考试草稿和制度通知稳定服务；响应保留历史业务字段并追加兼容元数据。学习完成链路使用事务和用户行锁保证课程首次积分只奖励一次，知识可见范围只由服务端员工上下文派生，考试显式旧状态版本返回 409，制度确认与阅读历史原子提交且发送使用 `policy.notify_send`。六域契约测试 7/7、权限联合定向测试 28/28、平台回归 105/105、全量 Node 回归 937/937、14 个相关 PHP 文件语法、平台预检和补丁格式检查通过；真实 MySQL、供应商与生产 HTTP 验收保留给隔离发布门禁，工作量和招聘并行冻结目录保持原边界。
  - [x] 12.2 迁移演练、技能、提醒、企微和专题域
    - 复用统一任务、文件和 AI 能力，保持业务状态机和历史结果语义。
    - 为每个域补齐契约、权限和回归测试。
    - 需求引用：需求 4、8、9、11；设计 3.3、13 波次 5。
    - 扩展 `PlatformBusinessDomainRegistry`，登记 `BIZ-006`、`BIZ-009`、`MSG-003`、`MSG-001`、`BIZ-019`、`BIZ-020`、`BIZ-021` 和 `BIZ-022` 的代表入口、版本、历史消费者与能力。演练、技能、提醒、企微和专题代表入口已接入 Kernel 请求上下文、统一认证或具名权限、兼容元数据及结构化审计；提醒手工运行与企微手工同步通过 `platform_jobs` 入队并保留 GET 查询动作，技能录音改用 `PlatformPrivateFileStorage` 且 Worker 同时解析私有引用和历史 URL，AI 仅复用既有 Drill Runtime。新增运营域契约测试 6/6，业务域、任务队列、文件、权限、AI、历史 Drill、运行时迁移、提醒角色和企微消费者定向回归累计 89/89，8 个相关 PHP 文件语法和补丁格式检查通过；真实 MySQL、生产 HTTP、企微与 AI 供应商联调保留给隔离发布门禁，工作量和招聘冻结边界保持不变。
  - [x] 12.3 接入工作量并行开发成果
    - 重新扫描工作量最新基线，核对并行改动、API 契约、数据表、Worker 和客户端。
    - 通过 Adapter 接入 Kernel、同步、文件、任务与健康能力，保持工作量领域事实和测试基线。
    - 需求引用：需求 1、4、7、9；设计 8、13 波次 5。
    - 已扩展 `PlatformBusinessDomainRegistry` 登记 `BIZ-001` 至 `BIZ-005`，并新增工作量平台 Adapter，将 `my-report.php` 与 `save-report.php` 接入 Kernel 请求上下文、认证、兼容元数据、结构化日志、等级 A 同步对象和持久化状态版本。显式旧 `state_version` 返回 409，历史客户端可继续省略该字段。异步导出与预警 Worker 通过 `workload.export.process`、`workload.alert.run` Handler 复用既有领域服务和 CLI；新导出写入平台私有根目录并执行临时导出、到期和受控路径策略，历史导出路径保持可读。核心请求与 Worker 已移除运行时 DDL，`202608020001_workload_platform_adapter.sql` 提供预警运行日志结构，readiness 缺失时 fail closed。首失败 Adapter 契约为 0/7，完成后 Adapter 7/7、平台与迁移定向回归 38/38、工作量回归 311/311、相关 PHP 语法和补丁格式检查均通过。治理 quick 门禁曾完整通过；最终并行工作树复跑时 277 个 PHP lint 全部通过，聚合 Node 阶段仅剩 Drill `home.php` 已迁移 Kernel 响应与旧 `drill_employee_api_contract.test.mjs` 断言之间的 1 项基线漂移，该文件不属于本任务边界。真实 MySQL 迁移、生产 HTTP 与 Worker 联调保留给隔离发布门禁。
  - [x] 12.4 接入招聘并行开发成果
    - 重新扫描招聘最新基线，接入统一 AI、OCR、文件、任务、提醒和录用转员工应用服务。
    - 外部处理审批缺失时保持真实数据处理门禁关闭。
    - 需求引用：需求 1、7-9、11；设计 3.3、7、13 波次 5。
    - 已扩展 `PlatformBusinessDomainRegistry` 登记 `BIZ-010` 至 `BIZ-013`，将招聘核心读写入口接入 Kernel、统一认证、具名权限、兼容元数据、结构化审计和状态版本。DeepSeek 文本生成与百度 OCR 通过平台 AI Runtime 执行，审批缺失时 fail closed；新简历写入平台私有文件资产，历史 `storage_key` 仅在二次鉴权下载入口受控兼容。招聘长任务通过 `recruitment.resume.process` Handler 复用权威处理服务，联系提醒和录用事件写入 transactional outbox。新增具名审批与幂等录用转员工服务，在同一事务内锁定投递和审批、校验状态版本、复用 `StaffLifecycleService`、持久化稳定响应并投影跨域事件。增量迁移 `202608020002_recruitment_platform_adapter.sql` 已登记到 `migration_catalog.php`，校验和为 `2844cc57585f33233e30b180455988811c5f73ef9ac6b09d1e54a28f1aa80657`，冻结 manifest 保持原边界。首失败 Adapter 契约为 0/9，完成后 Adapter 9/9、招聘全量回归 47/47、平台定向回归 50/50、47 个相关 PHP 文件语法、迁移 catalog 校验和及补丁格式检查均通过；真实 MySQL 迁移、生产 HTTP、真实 OCR/模型供应商和生产审批联调保留给隔离发布门禁。
  - [x] 12.5 完成 89 个功能组覆盖回归
    - 每个功能组关联可执行子项、自动测试、生产路径、验证证据和目标生命周期。
    - 覆盖公开官网、员工内网、Admin、PWA、小程序、PHP API 和独立 FastAPI 边界。
    - 需求引用：需求 1、2、13；设计 8、12、13。
    - 新增 `platform_function_coverage.mjs` 作为与 inventory 稳定 ID 双向一致的 89 项权威覆盖清单，每项显式记录端面、当前与目标生命周期、可执行子项、自动测试、静态证据、生产路径和发布验证状态。验证器对数量、重复、遗漏、未知 ID、生命周期漂移、证据缺失、非法状态和未阻断外部边界 fail closed，并已接入 `platform_preflight.mjs`。清单当前生命周期为 deployed 60、implemented 27、planned 2，目标为 verified 87、planned 2；发布验证保持 approval_required 59、blocked_external 30。45 个去重测试文件逐组执行 241/241 通过，全量 Node 回归 964/964、全仓 458 个 PHP 文件语法、迁移 catalog 与 inventory、平台预检、工作量 full 治理和补丁格式检查均通过。真实 FastAPI、MySQL、AI/OCR/语音供应商、企微、生产 HTTP、浏览器、微信开发者工具与真机、生产 Worker/Cron 和备份恢复演练继续作为受控发布门禁。

- [x] 13. 完善小程序页面与长期兼容治理
  - [x] 13.1 按稳定 API 完善小程序业务页面
    - 完成工作量、演练、学习、知识、考试、通关、制度、积分、通知和个人中心的统一状态回归。
    - 保持客户端展示层与服务端权威状态、权限和字段语义一致。
    - 需求引用：需求 4、6；设计 6、13 波次 6。
    - 实施证据：新增机器可读十域矩阵，覆盖首页、认证、档案、积分、排行、商城、打卡、知识、证书和反馈；`app.json` 登记 32 个页面，首页提供积分中心入口，积分聚合页接入概览、服务端权威排行、事务兑换和每日签到。`utils/view-state.js` 统一读取、写入、离线与冲突状态，商城和签到重试复用首次幂等键并提供明确恢复动作。十域矩阵与视图状态测试 4/4、加入积分 API 的定向测试 6/6、小程序相关回归 45/45、平台预检 2/2、全量 Node 回归 970/970 通过；相关 JavaScript 语法、`api/points/ranking.php` PHP 语法和补丁格式检查通过。微信开发者工具与真机的登录、Tab、页面渲染、网络切换、兑换、签到和冲突提示保留为发布门禁。
  - [x] 13.2 建立历史入口调用统计与退役门禁
    - 记录兼容入口调用量、消费者、迁移状态和截止条件。
    - 仅在消费者为零、契约回归通过和独立收缩批次批准后标记 `deprecated`。
    - 需求引用：需求 1.3-1.5、4.5、13；设计 9.1、14。
    - 实施证据：冻结 catalog 登记 29 条历史 endpoint、method、consumer、domain 与 owner 基线，Kernel 统一上下文按请求 ID 幂等记录调用收据和聚合计数，调用统计异常保持主响应可用，数据库退役判断在 schema 未就绪时以 `schema_not_ready` 阻断。增量迁移 `202608020003_platform_legacy_endpoint_governance.sql` 建立入口、调用收据、退役审批和不可变审计四表；四项具名权限分别控制查询、状态更新、提交和批准，审批使用行锁、提交人与批准人分离及完整证据、替代入口、owner、回滚计划、观察窗零调用门禁。首失败专用契约 0/8，完善并发审批后最终专用契约 9/9、平台定向回归 124/124、迁移契约 12/12、全量 Node 回归 979/979 通过；全仓 PHP 语法、inventory、89 项功能覆盖、平台预检和迁移 checksum 均通过。真实 MySQL、生产 HTTP、生产权限旅程、连续观察窗与独立收缩批次批准保留为发布门禁，本任务未执行入口删除或禁用。
  - [x] 13.3 执行全站自动化回归与发布预检
    - 运行 PHP 语法、Node 测试、迁移 dry-run、资产清单、PWA、小程序、Worker、文件、AI 和契约测试。
    - 输出波次验证证据、剩余环境门禁和回滚输入。
    - 需求引用：需求 10-13；设计 12、14。
    - 实施证据：新增配置驱动的 `platform_regression_preflight.mjs`、机器可读阶段配置和 fail-closed 契约测试，将 89 项 inventory/功能覆盖、全量 Node、全量 PHP 语法、迁移 catalog/checksum/compatibility/readiness dry-run、平台预检、权限矩阵、状态版本与同步、文件策略、任务租约与幂等、AI 审批与脱敏、历史入口治理、小程序十域状态、文档本地链接和补丁格式聚合为 17 个阶段，并按波次 0 至 6 输出名称、命令、耗时、状态和证据摘要。首轮契约因 runner 缺失按预期失败，完成后契约 4/4；完整预检本地阶段全部通过，全量 Node 193 个文件 983/983、全仓 PHP 465 个文件语法、迁移定向 13/13、权限 17/17、同步 5/5、文件 8/8、任务 15/15、AI 12/12、历史入口 9/9、小程序十域 6/6，57 个 Markdown 文件的 33 条本地链接与 `git diff --check` 通过。runner 清理数据库连接环境后执行 readiness dry-run，缺少 `DB_PASSWORD` 明确归类为 `blocked_external`；真实 MySQL 结构与数据核对、备份恢复、生产 HTTP/角色旅程、FastAPI、供应商、企微、浏览器、微信开发者工具与真机、Worker/Cron 和发布观察继续作为受控环境门禁，生产发布状态为 `approval_required`。回滚输入沿用应用版本恢复、expand-migrate-contract、变更日志、outbox、副作用回执及 `platform_release_gate.mjs` 的 15/30/60 分钟观察和停止阈值。

- [x] 14. 最终检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
  - 最终复跑证据：全站回归 runner 返回 `passed_with_gates` 且阻断阶段为 0；193 个 Node 测试文件 983/983、465 个 PHP 文件语法、89/89 功能覆盖、42 个迁移版本 checksum/compatibility、迁移定向 13/13、权限 17/17、同步 5/5、文件 8/8、任务 15/15、AI 12/12、历史入口 9/9、小程序十域 6/6、57 个 Markdown 文件的 33 条本地链接及 `git diff --check` 全部通过。数据库 readiness dry-run 因隔离环境未提供 `DB_PASSWORD` 保持 `blocked_external`，生产数据库核对、备份恢复、真实供应商、生产角色旅程、浏览器/真机、Worker/Cron 和发布观察保持 `approval_required`；这些受控环境门禁未被记作本地测试通过或实际生产发布。
