# 需求实施计划

- [x] 1. 固化迁移清单和云开发工程配置
  - [x] 1.1 扩展小程序业务域与路由清单
    - 将 `mini-program/business-domain-matrix.json` 扩展为覆盖 32 个页面、14 个迁移域、HTTP 方法、endpoint、认证、幂等、媒体字段和微信能力的权威清单。
    - 修正 Drill v2 反馈 endpoint，并标记考试分配等带副作用请求。
    - 需求引用：需求 1.1-1.5、3.2、10.6；设计 5.4、9、10。
  - [x] 1.2 配置云开发目录和环境占位
    - 新增 `mini-program/config/cloud.js`，配置环境 ID 占位、函数名、transport 和影子抽样率。
    - 修改 `mini-program/project.config.json` 增加实际 `cloudfunctionRoot`，修改 `app.js` 初始化 `wx.cloud`。
    - 需求引用：需求 3.1、9.1；设计 5.1、18、19。
  - [x] 1.3 建立云开发环境配置校验器
    - 编写静态校验脚本，检查环境 ID 占位、函数名、上游 origin、签名版本、云存储规则和基础库最低版本。
    - 将敏感配置限定为运行环境注入项，并对仓库中的真实密钥模式执行阻断检查。
    - 需求引用：需求 3.5-3.6、8.3；设计 11、18、19。
  - [x] 1.4 编写迁移清单和配置单元测试
    - 覆盖 32 个页面、14 个业务域、路由唯一性、方法登记、媒体字段和缺失环境配置。
    - 需求引用：需求 1.1、11.5；设计 9、15.1。

- [x] 2. 重构小程序统一请求传输层
  - [x] 2.1 定义 transport 接口并保留现有 API 公共契约
    - 在 `mini-program/utils/api.js` 内引入 transport 选择，保持 `request`、`uploadFile`、刷新、退出、请求 ID、幂等键和状态版本的页面调用方式。
    - 新增 `utils/transports/cloud.js`、`direct.js` 和 `shadow.js`。
    - 需求引用：需求 1.3、3.1、3.3-3.4、5.3-5.5、9.1；设计 5.2-5.3。
  - [x] 2.2 实现 cloud transport 响应与错误映射
    - 调用 `wx.cloud.callFunction`，将 `upstream_status` 和 `body` 转换为现有 `statusCode` 与 `data` 响应对象。
    - 将云网络、超时、协议、401、403、409、422 和 5xx 结果映射到现有稳定错误分类。
    - 需求引用：需求 3.4、5.4-5.5、8.4；设计 5.3、13。
  - [x] 2.3 将页面绝对 API 地址收口为相对路由
    - 修改首页、制度、学习、通知、通关、考试和知识页面，移除页面对 `globalData.apiBase` 的读取。
    - 保持页面状态、导航参数和业务字段语义。
    - 需求引用：需求 1.2-1.3、3.1；设计 5.4、9。
  - [x] 2.4 保持单飞刷新和一次重放语义
    - 将设备会话刷新与退出切换到 auth cloud transport。
    - 保持并发 401 共用一个刷新 Promise、延迟旧请求使用新令牌重放、每请求最多重放一次。
    - 需求引用：需求 4.5-4.7；设计 5.3、7.3。
  - [x] 2.5 编写 transport 单元与集成测试
    - 覆盖三种 transport、云响应包络、错误分类、超时、401 刷新、409 冲突和协议缺失。
    - 需求引用：需求 11.1；设计 15.1。
  - [x] 2.6 编写 direct 与 cloud 契约等价属性测试
    - 正确性属性 1：对相同员工、输入和版本生成固定响应，验证两种 transport 的业务码和数据等价。
    - 需求引用：需求 2.2、11.7；设计正确性属性 1。

- [x] 3. 实现固定路由的云函数 API 网关
  - [x] 3.1 创建 `api-proxy` 云函数骨架和版本化事件协议
    - 实现事件校验、route ID 解析、请求体限制、请求 ID 和稳定响应包络。
    - 建立云函数依赖、部署配置模板和本地 fixture 入口。
    - 需求引用：需求 3.1-3.5、8.1；设计 6.1。
  - [x] 3.2 实现路由白名单与参数 serializer
    - 依据权威路由清单登记 endpoint、方法、认证、超时、幂等和请求体上限。
    - 对查询、路径和请求体动态值执行类型、长度与允许值校验，固定 PHP 上游 origin。
    - 需求引用：需求 3.2、3.5；设计 6.1、11。
  - [x] 3.3 实现上游请求头传播和响应转换
    - 原样传播 Authorization、X-Request-ID、Idempotency-Key 和状态版本。
    - 转换 PHP HTTP 状态与 JSON 响应，限制响应大小并保留冲突恢复数据。
    - 需求引用：需求 3.3-3.4、5.3-5.5；设计 5.3、6.1。
  - [x] 3.4 实现 PHP 云网关签名校验
    - 新增 `api/cloud/GatewaySignature.php`，实现规范串、HMAC-SHA256、时间窗口、nonce 重放保护和签名版本轮换。
    - 云函数追加网关签名请求头，新增云专属 endpoint 强制校验签名。
    - 需求引用：需求 3.2、8.2-8.3；设计 6.3、11。
  - [x] 3.5 实现网关超时预算和脱敏日志
    - 为读取、写入、认证设置有界超时，将 AI 与媒体长任务路由到异步或云托管入口。
    - 记录 route ID、业务域、状态、耗时、请求 ID 和脱敏员工摘要。
    - 需求引用：需求 8.1-8.6；设计 6.4、14。
  - [x] 3.6 编写网关路由与签名单元测试
    - 覆盖允许路由、未知路由、错误方法、参数越界、签名错误、nonce 重放、响应过大和超时。
    - 需求引用：需求 11.1；设计 15.1。
  - [x] 3.7 编写幂等单结果属性测试
    - 正确性属性 2：相同幂等键与相同请求摘要最多形成一个权威结果。
    - 需求引用：需求 5.3；设计正确性属性 2。
  - [x] 3.8 编写幂等冲突属性测试
    - 正确性属性 3：相同幂等键与不同请求摘要返回稳定冲突。
    - 需求引用：需求 5.3-5.4；设计正确性属性 3。
  - [x] 3.9 编写权威数据源边界属性测试
    - 正确性属性 4：随机业务写入只调用现有 PHP/MySQL 业务路径，云数据库保持业务事实空集。
    - 需求引用：需求 2.1-2.4；设计正确性属性 4。

- [x] 4. 实现双登录、绑定票据和设备会话云入口
  - [x] 4.1 创建 `auth-proxy` 云函数和认证路由白名单
    - 登记密码登录、微信登录、微信绑定、企微兼容登录与绑定、设备会话刷新和退出。
    - 对密码、微信 code、绑定票据和 Token 使用内存中转及独立脱敏日志策略。
    - 需求引用：需求 4.1-4.7、8.3；设计 6.2、7.1。
  - [x] 4.2 实现一次性微信绑定票据
    - 新增版本化增量迁移，保存票据摘要、员工、会话、到期和消费状态。
    - 修改 `api/auth-jwt.php`，在密码登录后签发短期票据，并在绑定事务中锁定、消费票据和递增会话版本。
    - 需求引用：需求 4.3-4.4、10.4；设计 7.2。
  - [x] 4.3 修改小程序绑定流程保存短期票据
    - 修改 `utils/auth.js`、登录页和绑定页，以 `pendingWechatBindTicket` 替换跨页账号密码。
    - 处理票据过期、已消费、OpenID 冲突和重新认证状态。
    - 需求引用：需求 4.3-4.4、10.4；设计 7.2。
  - [x] 4.4 接入微信与密码双登录云路径
    - 微信登录继续向现有 PHP 传递 `wx.login` code，密码登录继续使用 WordPress 与员工映射。
    - 保持设备 ID、session type、refresh rotation、员工状态和会话版本契约。
    - 需求引用：需求 4.1-4.2、4.5-4.7、7.1；设计 7.1、7.3。
  - [x] 4.5 编写认证云入口集成测试
    - 覆盖双登录、待绑定、票据过期、票据重放、OpenID 冲突、刷新轮换、令牌复用、退出和员工停用。
    - 需求引用：需求 11.2；设计 15.1。
  - [x] 4.6 编写刷新凭据单次消费属性测试
    - 正确性属性 6：任意并发刷新序列中，每个刷新凭据最多成功消费一次。
    - 需求引用：需求 4.5-4.7；设计正确性属性 6。
  - [x] 4.7 编写绑定票据单次消费属性测试
    - 正确性属性 7：任意绑定重放序列中，每个票据只关联一个员工和会话并产生一次有效消费。
    - 需求引用：需求 4.3-4.4；设计正确性属性 7。
  - [x] 4.8 编写权限范围属性测试
    - 正确性属性 5：对随机角色、门店和员工上下文，云路径结果始终位于服务端授权范围内。
    - 需求引用：需求 5.1-5.2；设计正确性属性 5。

- [x] 5. 检查点：确保已选择的基础网关与认证测试通过，如有疑问请询问用户
  - [x] 5.1 运行基础迁移检查并修复阻断结果
    - 执行配置、transport、网关、签名、认证、会话和权限相关的已选择自动测试。
    - 执行增量迁移 checksum、PHP 语法、JavaScript 语法和补丁格式检查。
    - 已由全量 Node 回归、PHP lint、迁移 catalog/compatibility、平台预检和统一回归预检覆盖；当前本地必需门禁通过。
    - 需求引用：需求 11.1-11.2；设计 15、16 波次 0-2。

- [x] 6. 修正迁移基线业务偏差并迁移普通业务域
  - [x] 6.1 修正制度通知确认标识
    - 修改制度详情和通知页面，使用通知记录 ID 执行确认并保留制度关联 ID。
    - 在 PHP 服务中校验通知归属、制度关系和当前员工权限。
    - 需求引用：需求 10.1；设计 10.1。
  - [x] 6.2 统一通关阶段参数和协议状态
    - 将学习与通关页面统一为 `stage_id`。
    - 将应用启动和登录页协议状态统一为版本化键，并实现旧状态一次迁移。
    - 需求引用：需求 10.2-10.3；设计 10.2-10.3。
  - [x] 6.3 实现知识示范音频播放器状态
    - 使用统一媒体描述和播放器实现播放、暂停、错误、完成与页面卸载清理。
    - 需求引用：需求 10.5、7.5；设计 8.4、10.5。
  - [x] 6.4 将考试分配改为幂等 POST
    - 修改小程序考试页和 PHP 分配入口，要求 `Idempotency-Key` 并稳定返回首次分配结果。
    - 保持考试详情、题目、恢复、保存和提交现有字段。
    - 需求引用：需求 10.6、5.3；设计 10.6。
  - [x] 6.5 迁移首页、制度、通知、学习、知识和通关读取
    - 将只读请求通过 `api-proxy` 执行，保持分页、能力版本、状态和导航语义。
    - 增加媒体字段标准化入口，为后续镜像任务返回稳定资产键。
    - 需求引用：需求 1.2-1.5、2.1-2.4、5.1-5.5；设计 9、16 波次 3。
  - [x] 6.6 迁移档案、积分、考试、知识进度和工作量写入
    - 将档案更正、改密、签到、兑换、考试保存提交、知识进度和日报保存提交接入云路径。
    - 保持 Idempotency-Key、状态版本、409 恢复和服务端权威回读。
    - 需求引用：需求 1.2、2.4-2.5、5.3-5.5；设计 9、16 波次 4。
  - [x] 6.7 迁移提醒订阅记录
    - 保持 `wx.requestSubscribeMessage` 在客户端执行，将授权结果通过云路径写入现有提醒 API。
    - 保留稍后设置、再次申请和模板场景映射。
    - 需求引用：需求 7.2-7.3；设计 9。
  - [x] 6.8 编写普通业务域契约测试
    - 覆盖制度确认、通关参数、协议迁移、考试分配、积分幂等、工作量状态版本和提醒授权记录。
    - 需求引用：需求 10.1-10.6、11.7；设计 15.1。

- [x] 7. 实现云存储、媒体适配和历史媒体镜像
  - [x] 7.1 创建统一小程序媒体工具
    - 新增 `mini-program/utils/media.js`，生成受控云路径、上传文件、解析标准媒体描述、下载音频临时路径和清理缓存索引。
    - 将工作量、学习、知识、积分、头像和演练页面接入统一媒体工具。
    - 需求引用：需求 6.1、6.4、7.5；设计 8.1、8.4。
  - [x] 7.2 创建 `media-ticket` 云函数
    - 校验员工、业务用途、业务对象、fileID、MIME 声明、大小、摘要和幂等键。
    - 创建新媒体转发或历史媒体镜像任务，并返回稳定任务标识和状态。
    - 需求引用：需求 6.2-6.7；设计 8.1、8.3。
  - [x] 7.3 实现 `media-adapter` 云托管服务
    - 创建流式云文件读取、真实 MIME 与 SHA-256 校验、大小限制、PHP 受信转发和有限重试。
    - 为工作量 `image_file`、演练音频和通用媒体登记实现业务适配器。
    - 需求引用：需求 6.2-6.4、6.6-6.7、8.5；设计 8.2、11、13。
  - [x] 7.4 新增 PHP 媒体接收入口和映射模型
    - 新增 `api/cloud/media-ingest.php`，组合网关签名、员工权限、文件策略和现有领域服务。
    - 新增 `platform_cloud_media_mappings` 增量迁移及数据访问服务，保存资产键、业务对象、云文件、摘要和状态。
    - 需求引用：需求 2.3、6.2-6.5；设计 8.2-8.3。
  - [x] 7.5 实现历史媒体按需镜像和预热工具
    - 首次访问时幂等创建镜像任务，ready 映射直接复用，失败映射返回可恢复任务状态。
    - 编写有界预热 CLI，根据输入资产清单复制常用课程、知识和积分媒体。
    - 需求引用：需求 6.5、6.7；设计 8.3。
  - [x] 7.6 实现媒体任务状态与过期恢复
    - 保存 pending、ready、failed、expired 状态、重试次数和稳定错误码。
    - 处理云文件失效、历史源变化、摘要变化和并发镜像请求。
    - 需求引用：需求 6.5-6.7、8.5；设计 8.3、13。
  - [x] 7.7 编写媒体单元与集成测试
    - 覆盖 512B、5MB 边界、错误 MIME、摘要不一致、重复上传、并发镜像、源变化、云文件失效和任务恢复。
    - 需求引用：需求 11.3；设计 15.1。
  - [x] 7.8 编写媒体摘要一致性属性测试
    - 正确性属性 8：对随机文件和重试序列，ready 映射摘要始终等于云文件内容摘要。
    - 需求引用：需求 6.2-6.5；设计正确性属性 8。

- [x] 8. 迁移 Drill v2 录音、分片和 AI 恢复链路
  - [x] 8.1 将新录音上传切换到云存储
    - 修改 `utils/drill-v2.js` 和演练页面，通过统一媒体工具上传音频并登记实例、格式、大小和摘要。
    - 保持录音授权、暂停、恢复和页面清理逻辑。
    - 需求引用：需求 6.1-6.3、6.6、7.4；设计 8、9。
  - [x] 8.2 适配 Drill v2 音频领域协议
    - 让 media adapter 调用现有音频资源、分片和 finalize 服务，保持 attempt 归属、分片序号、总数、SHA-256、状态版本和幂等键。
    - 将大音频改为流式处理，减少 Base64 中间载荷。
    - 需求引用：需求 6.6-6.7、5.3-5.4；设计 8.2、12.9。
  - [x] 8.3 迁移演练结果、学习反馈和音频播放
    - 将演练首页、目录、任务、进度、结果、学习、状态和最终提交接入云入口。
    - 将历史反馈音频解析为云媒体描述并使用本地临时路径播放。
    - 需求引用：需求 1.2、6.4-6.5、7.5；设计 9、16 波次 6。
  - [x] 8.4 保持 AI 长任务与失败恢复
    - 将耗时评分和转写交给现有 Worker 或云托管任务，返回稳定处理中、失败和恢复状态。
    - 保持请求 ID、幂等副作用标识、AI 错误分类和结果轮询。
    - 需求引用：需求 2.5、8.2、8.5；设计 6.4、13。
  - [x] 8.5 编写 Drill v2 云路径集成测试
    - 覆盖同幂等键重试、不同请求复用同键、状态冲突、分片缺失、重复分片、乱序分片、finalize 重试和 AI 超时。
    - 需求引用：需求 11.4；设计 15.1。
  - [x] 8.6 编写 Drill 媒体协议属性测试
    - 正确性属性 9：对随机分片排列和重试序列，成功提交始终满足分片集合、摘要和状态版本契约。
    - 需求引用：需求 6.6；设计正确性属性 9。
  - [x] 8.7 收口 AI 销售演练体验契约
    - 修复模块步骤状态展示、自由对话多轮状态版本和 AI 家长回复字段。
    - 支持家长画像筛选、随机画像、课程标签匹配、画像快照冻结和结束评分入口；筛选项由业务域字典提供并在服务端校验有效性。
    - 评分结果页轮询异步评估，展示总分、维度得分、薄弱项和改进建议。
    - 新增销售演练专项审计测试，并接入平台预检和分阶段回归门禁。
  - [x] 8.8 补齐销售演练五维画像生产字典与发布门禁
    - 新增幂等数据迁移，为 `new_signing` 业务域补齐五维 active 画像字典，同时保留旧画像维度。
    - 在迁移 catalog 中登记固定 checksum 和 `expected_zero` readiness 检查，阻断画像种子缺失的发布。
    - 扩展销售演练与迁移专项测试，验证五维字典种子和发布门禁契约。
    - 验证：销售演练专项 6/6、迁移 readiness 3/3、内容治理 7/7，平台预检 `passed` 且阻断项和警告均为 0。

- [x] 9. 实现影子核对、传输切换和回滚兼容
  - [x] 9.1 实现只读请求影子核对
    - cloud 结果作为用户响应，对登记为只读的请求按配置抽样执行 direct 对照。
    - 比较 HTTP 语义、业务码、字段结构、权限范围和脱敏业务摘要，保存稳定差异分类。
    - 需求引用：需求 9.1-9.2、11.7；设计 5.2、14、16 波次 7。
  - [x] 9.2 实现版本化传输开关
    - 支持按环境、客户端版本和紧急配置选择 cloud、direct 或 shadow。
    - 对未知模式、过期配置和能力版本差异使用批准的安全默认值。
    - 需求引用：需求 9.1、9.3-9.4；设计 5.2、16。
  - [x] 9.3 实现迁移结构的 N/N-1 兼容
    - 绑定票据和媒体映射采用 expand-migrate-contract，旧客户端继续读取兼容字段。
    - 编写保留式回滚计划生成器，核对在途媒体任务和已提交 MySQL 事实。
    - 需求引用：需求 9.4-9.6；设计 17。
  - [x] 9.4 扩展发布门禁的自动停止判定
    - 将云错误率、延迟、影子差异、媒体任务积压和核心流程结果接入现有 release gate 输入模型。
    - 输出可机器读取的继续、停止和回滚评估结果。
    - 需求引用：需求 9.4、11.8；设计 14、16-17。
  - [x] 9.5 编写影子核对与切换测试
    - 覆盖只读抽样、写请求单路径、敏感字段脱敏、差异分类、配置过期和紧急回滚。
    - 需求引用：需求 9.1-9.6、11.7-11.8；设计 15.1。
  - [x] 9.6 编写回滚兼容属性测试
    - 正确性属性 10：对随机迁移写入和回滚序列，N-1 读取保留已提交 MySQL 事实和兼容字段。
    - 需求引用：需求 9.5-9.6；设计正确性属性 10。

- [x] 10. 扩展小程序自动化门禁和全量回归入口
  - [x] 10.1 扩展小程序静态契约检查器
    - 修改 `scripts/check_miniprogram_contracts.mjs`，验证 32 页注册、导航、相对 API、transport、设备会话、媒体工具、云环境和路由清单一致性。
    - 对页面直接使用 `wx.request`、`wx.uploadFile`、绝对业务 URL 和未登记媒体字段输出阻断结果。
    - 需求引用：需求 11.5；设计 15.1、18。
  - [x] 10.2 将云开发契约接入平台预检
    - 修改 `scripts/platform_preflight.mjs` 和全站回归配置，聚合网关、认证、媒体、Drill、迁移和回滚兼容检查。
    - 输出本地通过项与外部云环境、开发者工具和真机条件的分离状态。
    - 需求引用：需求 3.6、11.1-11.8；设计 15-16。
  - [x] 10.3 编写小程序自动化主流程测试
    - 使用微信开发者工具自动化覆盖双登录、首页、制度、通知、学习、知识、考试、通关、积分、工作量、提醒、个人中心和演练主流程。
    - 已新增本地自动化覆盖计划、真实 DevTools runner 启动参数、测试身份/登录态门禁、产物路径和 fail-closed 校验；真实执行需提供 `WECHAT_DEVTOOLS_CLI`、`MINIPROGRAM_AUTOMATION_ARTIFACT_DIR`、测试账号或登录态后运行。
    - 需求引用：需求 11.6；设计 15.2。
  - [x] 10.4 编写弱网、并发和恢复自动化测试
    - 覆盖云函数超时、并发 401、媒体断点、AI 处理中、页面恢复、409 冲突和 transport 回滚。
    - 已新增本地恢复场景计划、`MINIPROGRAM_NETWORK_PROFILE` 外部门禁和自动化产物完整性校验；弱网、真机和 DevTools 执行环境仍作为外部门禁。
    - 需求引用：需求 2.5、6.7、8.4-8.5、9.4；设计 13、15.2。

- [x] 11. 检查点：确保全部已选择测试通过，如有疑问请询问用户
  - [x] 11.1 执行全量静态与自动回归并修复阻断结果
    - 运行 JavaScript 与 PHP 语法、增量迁移、transport、网关、认证、业务域、媒体、Drill、影子核对、回滚兼容和平台预检。
    - 确认需求 1-11、设计正确性属性 1-10 和 32 个页面均有实现与自动证据映射。
    - 已通过 `node --test scripts/*.test.mjs`，结果为 1162/1162 通过；全量 PHP 语法检查 486/486 通过，迁移兼容检查 56/56 通过。
    - 已通过 `node scripts/platform_preflight.mjs`，无 blocking issues、无 warnings，包含 10/10 份 `mini_program_cloudbase_contracts` 证据。
    - 已通过 `node scripts/platform_regression_preflight.mjs`，状态为 `passed_with_gates`；本地必需阶段通过，`migration_readiness_dry_run` 与 `miniprogram_devtools_automation` 保持外部环境门禁，DevTools 证据摘要列出四项缺失条件代码。
    - 已通过 `git diff --check`。
    - 真实微信开发者工具、真机弱网和测试账号/登录态条件仍由 10.3、10.4 的外部门禁跟踪。
    - 需求引用：需求 11.1-11.8；设计 12、15-17。
