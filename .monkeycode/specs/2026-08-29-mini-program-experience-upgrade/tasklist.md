# 需求实施计划

- [ ] 1. 建立销售模块与试卷查询契约
  - [x] 实现按 `course_id` 关联课程名称的销售模块映射读取。
  - [x] 为基础/专业类型增加服务端映射结果。
  - [x] 新增员工可访问的考核列表接口，返回启用状态、模块、类型、题量、时长和及格线。
  - [x] 新增当前员工考核历史接口，返回分数、通过状态、模块和完成时间。
  - 覆盖需求 6.1-6.2、6.6-6.7和设计中的试卷盘点门禁。
  - 为试卷筛选、员工权限和空试卷结果编写单元测试。

- [ ] 2. 调整小程序底部导航和首页入口
  - [x] 修改 `mini-program/app.json`，将底部 Tab 调整为工作量、演练、数据中心。
  - [x] 新增数据中心页面注册和导航路径。
  - [x] 保持工作量页面、接口和店长管理路径不变。
  - 扩展首页待办类型映射、本月事项展示和业务跳转。
  - 覆盖需求 1.1-1.5、2.1-2.6和设计正确性属性 1、2、7。
  - 为 Tab 注册、首页待办空状态、错误状态和未登录零请求编写契约测试。

- [ ] 3. 重构演练首页与销售入口
  - [x] 在现有演练列表页面增加销售 Q&A、销售流程演练和考核三个入口。
  - [x] 保留已有任务列表、自由练习、继续练习和历史演练路径。
  - 创建销售 Q&A 列表和练习页面，接入统一请求层。
  - 创建销售流程阶段列表和过程页面，复用现有演练实例状态恢复。
  - 覆盖需求 3.1-3.5、5.1、5.4-5.5。
  - 为入口路由、继续练习、录音失败和文本兜底编写页面契约测试。

- [ ] 4. 接入 Q&A 与销售流程 AI 评分
  - [x] 将 Q&A 评分维度写入 AI 评分模式提示。
  - [x] 将销售流程评分维度写入 AI 评分模式提示。
  - [x] 复用现有演练 AI 适配层、结构化 `dimension_scores`、证据和建议字段。
  - 为 Q&A 增加回答提交、评分状态、结构化结果和历史结果查询。
  - 为销售流程增加阶段回答保存、流程提交、综合评分和阶段反馈展示。
  - 保留 AI 失败时的原始回答、阶段状态和重试能力。
  - 为新增写操作接入幂等键并复用统一错误分类。
  - 覆盖需求 4.1-4.5、5.1-5.5和设计正确性属性 4、5、8。
  - 为四项 Q&A 评分维度和四项流程评分维度编写评分服务单元测试。
  - 为 AI 超时、结构化结果缺失、重复提交和重试状态编写集成测试。

- [ ] 5. 接入小程序考核流程
  - [x] 创建考核列表页面，按销售模块和基础/专业类型筛选试卷。
  - [x] 复用现有 `pages/exam/exam.js` 的计时、语音转写、自动保存、恢复和提交能力。
  - [x] 补充考核入口传递试卷信息的导航参数。
  - [x] 接入现有试卷列表和历史接口。
  - 展示分数、及格状态、所属模块和完成时间。
  - 覆盖需求 6.2-6.7和设计正确性属性 6。
  - 为试卷列表、暂存恢复、提交结果、权限失败和下架试卷编写集成测试。

- [ ] 6. 实现数据中心
  - [x] 创建数据中心页面和统一的数据聚合读取入口。
  - [x] 默认查询本月数据，并支持月份切换。
  - [x] 展示个人演练、考核和工作量摘要。
  - 根据后端角色展示店长授权门店的工作量汇总和员工明细入口。
  - 对各数据块采用独立加载、空状态和错误状态。
  - 覆盖需求 7.1-7.7和设计数据模型 `personal_summary`、`manager_summary`。
  - 为员工与店长数据范围、月份切换、局部失败和空数据编写接口测试。

- [x] 7. 扩展“我的”页面
  - [x] 保留个人资料、密码修改、资料更正、清除缓存、检查更新和退出登录。
  - [x] 增加数据中心、演练历史、考核历史和知识收藏入口。
  - [x] 统一使用 `utils/navigation.js` 处理 Tab 和普通页面跳转。
  - 覆盖需求 8.1-8.5。
  - 为个人资料加载失败、历史入口和收藏入口编写页面契约测试。

- [ ] 8. 更新小程序发布契约和回归门禁
  - [x] 更新页面路由、Tab 清单、统一请求层和能力版本检查。
  - 增加工作量冻结范围检查，阻止本轮误改工作量业务逻辑。
  - 执行小程序路由、核心范围、演练、考试和数据中心相关测试。
  - [x] 执行 JavaScript 语法检查和 `git diff --check`。
  - 运行现有工作量契约与回归测试，确保冻结模块行为一致。

- [ ] 9. 检查点 - 确保所有测试通过,如有疑问请询问用户
