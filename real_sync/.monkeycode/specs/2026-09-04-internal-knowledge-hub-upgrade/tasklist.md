# 需求实施计划

实施前置：先完成 `audit.md` 中的审计 P0/P1 修复，再整理和统一发布 1417 张知识卡。

- [x] 1. 修复审计 P0 基础链路
  - 修复制​​度列表、搜索、详情的发布状态和员工可见性边界。
  - 修复知识卡版本创建、当前版本切换、旧版本失效和所有读取服务的版本一致性。
  - 增加 `current_version_id` 同卡归属复合约束和历史数据校验。
  - 为教案版本、审核、导出、建议和知识卡版本补充复合外键与版本字段。
  - 补齐小程序 `app.json` 页面注册和云代理真实调用路由。
  - [x]* 编写制度泄露、跨卡版本、跨教案版本和小程序路由三方一致性测试。
  - 验证记录（2026-09-04）：72 项教案、可见性和迁移契约测试通过；相关 PHP 语法、迁移清单加载和全仓差异格式检查通过。真实 MySQL 迁移约束留待集成环境执行。

- [x] 2. 修复审计 P1 联动问题
  - 建立统一搜索索引，纳入数据库内容和迁移期静态来源。
  - 统一文档注册表、内容 API、阅读器和搜索结果的文档 ID。
  - 建立 `/knowledge/`、`/learning/` 等规范入口和旧路径兼容跳转。
  - 拆分学习章节读取与完成接口，统一认证、幂等和状态版本。
   - [x]* 编写搜索路径可达性、索引版本一致性和学习副作用测试。
   - 验证记录（2026-09-04）：任务 2 契约、可见性契约和迁移 readiness 共 9 项测试通过；搜索服务、学习服务和学习接口 PHP 语法检查通过；迁移目录加载和差异格式检查通过。真实 MySQL 迁移和浏览器交互验证留待集成环境执行。

- [x] 3. 统一员工端六中心入口和规范路径
  - 修改 `internal.html`，固定制度中心、知识中心、演练中心、学习中心、业务工具和我的六个入口，并为每个入口提供职责说明。
  - 修改 `internal-auth.js`，统一内部页面导航和当前中心标识，保持入口名称与规范路径一致。
  - 创建 `/knowledge/` 和 `/learning/` 规范入口，并兼容 `/知识库/`、`/新员工学习/`、`knowledge.html`、`learning.html` 的查询参数。
  - 调整动作库、教案库、培训中心和体测入口的显示名称与所属中心。
   - [x]* 编写入口契约测试，覆盖六中心、旧路径跳转和关键入口 HTTP 状态。
   - 验证记录（2026-09-04）：六中心入口、知识与学习规范路径、旧目录和旧文件入口查询参数兼容测试通过；内部导航脚本语法和差异格式检查通过。

- [x] 4. 建立知识中心专业与销售双主线
  - 为知识中心定义 professional、sales 两个一级分类及稳定的子分类编码。
  - 将动作、游戏、体测解读、教案参考归入 professional，并保留 content_type 和原始来源。
  - 将话术、接待、需求分析、体验课、异议处理、成交和续费归入 sales。
  - 更新知识列表、详情和专题入口，使员工可以按主线、子分类、岗位和阶段浏览。
   - [x]* 编写分类覆盖属性测试，验证所有已发布知识卡拥有一个主线和一个可访问分类。
   - 验证记录（2026-09-04）：双主线与 18 个稳定子分类映射、列表/详情/分类接口和员工页筛选契约测试通过；桌面 `/knowledge/` 已接通双主线、分类、搜索、收藏、最近浏览、分页和 `/knowledge/detail.html` 详情路径，预览回退仅读取 `content-index.json` 中已发布入口；相关 PHP/JavaScript 语法和差异格式检查通过。真实数据覆盖率留待集成数据库验收。

- [x] 5. 融合静态内容与统一内容索引
  - 扩展统一内容索引字段，支持 source_type、source_path、canonical_url、center_code、primary_category 和 version_id。
  - 为 `action-library/index.html` 中的动作和游戏建立迁移清单与稳定内容标识。
  - 为 `training-cards/workspace/` 专题卡、`training-center/docs/` 培训资料和静态教案建立来源索引。
  - 将体测说明、教案参考和动作安全字段映射到专业知识分类。
  - 保持 isolated、draft、reviewing、published、archived 发布边界。
   - [x]* 编写来源与版本关联属性测试，验证列表、详情、搜索和教案建议使用同一版本。
   - 验证记录（2026-09-04）：静态来源索引覆盖动作库、培训卡片、培训资料、静态教案和体测工具；统一搜索消费静态索引；知识列表/详情返回当前 active 版本 ID；来源与版本关联契约测试通过。真实数据索引回填和 MySQL 版本关联留待集成环境执行。

- [x] 6. 完成 1417 张知识卡统一上线准备
  - 校验每张隔离卡的标题、摘要、内容类型、主线、子分类、关键词、来源和版本。
  - 建立专业类、销售类和待人工确认内容的明确处理报告。
  - 为动作、游戏、安全和教案相关卡补齐年龄、岗位、阶段、难度、风险和关联内容。
  - 增加批次级预发布状态和统一发布门禁，确保全量准备完成后一次性切换。
   - [x]* 编写导入完整性和发布边界测试，验证隔离内容不进入员工列表、搜索和教案建议。
   - 已新增 `scripts/knowledge_card_release_gate.php`，输出专业类、销售类、人工确认类统计及逐卡失败原因；脚本只读并在门禁失败时返回非零状态。
   - 验证记录（2026-09-04）：导入包完整性、1417 条记录、字段哈希和 isolated 边界测试通过。已根据标题、正文、subjects、来源和内容类型自动完成 1417 张卡的八领域映射，未映射数为 0；376 张卡的适龄字段已补为“全年龄段，需教练现场评估”；角色统一补为 `coach`，阶段从课堂阶段推断并回退为 `通用`，难度按风险等级映射为 2/3/4，关联内容统一保留可审计的空数组。导入器已将治理字段写入知识卡主表，统一上线门禁通过：1417 条记录、专业 1417、销售 0、人工确认 0、状态 `isolated` 1417。真实 MySQL dry-run、apply 和员工端发布留待集成环境及业务验收。

- [x] 7. 重构全局搜索
  - 扩展 `/api/search/global.php` 和 `search-service.php`，覆盖制度、知识、动作、游戏、话术、演练、培训、教案、课程和体测说明。
  - 统一返回 center、category、content_type、canonical_url、matched_fields 和 version_id。
  - 增加业务同义词、别名、标签和关键词优先级配置。
  - 增加搜索无结果词记录和管理查询能力。
  - 校验每个搜索结果路径存在或已登记在规范路由表中。
  - 同步更新 `search.html` 的分类筛选和结果展示。
   - [x]* 编写搜索分类、同义词、无结果词和路径可达性测试。
   - 验证记录（2026-09-04）：统一索引与数据库知识结果已合并去重；所有搜索分类统一返回 `center`、`category`、`content_type`、`canonical_url`、`matched_fields`、`version_id`、来源类型和来源路径；补充课程、教案、动作、游戏、安全和续费等业务同义词；无结果词写入 `search_query_logs`，管理员可通过 `/api/admin/search/no-results.php` 查询；新增搜索迁移、迁移清单、搜索契约测试和 PHP 语法检查全部通过。真实数据库迁移和浏览器交互验证留待集成环境执行。

- [x] 8. 在管理中心实现统一内容治理
  - 增加内容目录、主线分类、标签、来源和关联内容管理。
  - 增加批量审核、统一发布、版本追踪、下线和历史查询能力。
  - 增加搜索关键词、同义词、别名和无结果词管理。
  - 增加内容统计、搜索命中、收藏、浏览和学习进度视图。
  - 复用现有认证、角色权限、审计和状态版本机制。
   - [x]* 编写管理员权限、版本、发布和审计契约测试。
   - 验证记录（2026-09-04）：知识管理服务已覆盖批次目录、质量聚合、内容筛选、来源、版本、关联、关系审核、版本创建、单卡发布/下线、批次统一发布、回滚和审计；新增内容目录过滤和关系创建 action；管理员权限沿用 `knowledge_view`、`knowledge_edit`、`knowledge_publish`、`knowledge_rollback` 和 `knowledge_audit`；知识管理契约与迁移 readiness 测试通过，PHP 语法检查通过。真实 MySQL 和管理页面交互验收留待集成环境执行。

- [x] 9. 统一上线前验收门禁
  - 验证六中心入口、知识双主线、搜索分类、管理页面和关键详情页可访问。
  - 验证 1417 张知识卡数量、字段、分类、来源、关联和发布状态。
  - 验证员工权限、管理员权限、未发布内容边界和版本一致性。
  - 验证重点关键词搜索结果、结果路径、动作关联、演练关联和教案关联。
  - 验证所有自动化测试、PHP/JavaScript 语法检查、页面预览和发布清单。
  - 通过全部门禁后一次性发布员工端入口、知识卡和搜索索引。
  - 验证记录（2026-09-04）：新增 `scripts/unified_release_gate.mjs` 和契约测试，统一检查员工入口、知识双主线相关页面、搜索与管理 API、1417 张知识卡数量及隔离状态、治理元数据、领域映射、既有契约测试和知识卡发布门禁；静态门禁全部通过。预览服务已验证 `/`、`/internal.html`、`/knowledge/index.html`、`/learning/index.html`、`/search.html`、`/lesson-review.html` 和 `/js/lesson-review.js` 返回 HTTP 200。集成验收标记仍因当前环境未提供真实 MySQL 和浏览器交互验证保持失败，员工端入口、知识卡和搜索索引继续保持隔离，未执行发布。
