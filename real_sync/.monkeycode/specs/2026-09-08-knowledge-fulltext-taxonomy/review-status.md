# 全文分类审阅进度

状态：全部1647条已确认归并，已生成全量发布快照，待完成生产部署和只读验证。

## 覆盖与校验

- 生产快照：1647卡，正文1172195字符。
- 已有逐卡AI提案：1647条；缺少提案：0条。
- 每条提案附知识ID、版本ID、正文SHA256、主主题、二级描述、辅助标签、引文和分类理由。
- 已有提案中222条标记分类待复核；专业性、正文缺失等质量标记另行保留。
- 最新逐卡校验记录的ID、版本、必填项、状态及精确引文问题为0。该校验未全面覆盖引文长度及CSV列数，也不代表分类判断已获专业确认。
- 本轮独立核验已生成的全量目录映射：1647条，无遗漏、重复、额外ID、CSV结构或父子代码错误。

## 当前主主题提案

| 主主题 | 已有提案数 |
|---|---:|
| 专项运动技能 | 331 |
| 力量与稳定性 | 292 |
| 基础动作与身体控制 | 220 |
| 速度敏捷与反应 | 174 |
| 柔韧与活动度 | 105 |
| 感觉统合 | 97 |
| 认知注意与执行功能 | 69 |
| 同伴互动与社会性 | 58 |
| 情绪与心理支持 | 51 |
| 教学设计与教练方法 | 59 |
| 耐力与体适能 | 35 |
| 家校协同与家庭支持 | 33 |
| 儿童生长与发育 | 30 |
| 课堂组织与行为支持 | 30 |
| 安全防护与健康边界 | 24 |
| 体测与观察评估 | 23 |
| 教练职业发展 | 16 |

## 分类边界

主题反映主要训练目标或主要解决的问题。内容形式保留为独立筛选维度。感觉统合依据明确的感觉输入、辨别、调节或感觉相关课堂支持目标细分；普通力量、跳跃和平衡依据运动目标分类。跨主题卡片选择主目标并附辅助标签。家长反馈作为示例时沿用知识主主题。

原始二级描述共1498种，包含大量动作级描述与近义词。taxonomy-catalog.json已定义17个主主题下的稳定目录，1647条归并映射已生成。

## 文件

classification-proposals.csv：逐卡草案。
pending-review.csv：缺少分类提案的清单，当前仅表头；222条分类待复核记录仍在逐卡草案中。
review-progress.json：逐卡提案统计与校验结果。
normalization-progress.json：本轮目录归并覆盖、结构检查与阻塞状态。
normalization-inputs/group-*.csv：六组已生成归并结果的项目内归档，保留原始字节。
normalized-proposals.csv：1311条首批归并提案，关联原始版本、正文哈希、引文、理由及复核标记，附新目录代码和来源组。
pending-normalization.csv：336条原待归并记录，现已依据用户确认的逐卡提案转换为稳定目录代码并纳入发布快照。
normalization-summary.json：部分结果统计与输入文件哈希；当前使用66个二级目录，包含20条主主题调整提案，均附原有调整说明。六组中166条仍需分类复核；全量待分类复核仍为222条。
review/index.html：只读审阅页，展示全量1647条既有提案，明确区分1311条已归并与336条待归并。
review/data.json：由汇总脚本生成的页面数据；待归并记录不填充新分类代码。

页面支持主题、二级目录、处理状态、质量标记与文本搜索，支持逐卡引文和追溯信息展开、分页及完整筛选结果JSON导出。审阅页操作不修改分类记录或生产数据。

## 1647条部署状态

- 发布版本：`fulltext-20260908-approved-1647-v3`。
- 部署范围：1个版本化分类快照、2个分类服务类、3个知识API入口、4个知识页面资源。生产备份目录：`/tmp/knowledge-taxonomy-backup-20260908`。
- 匹配策略：知识ID、当前版本ID、正文SHA256同时匹配；任一发生变化时回退既有分类推导。
- 生产只读核验：当前已发布1647卡；全量匹配1647条，版本/正文哈希冲突0；专业知识列表1647条，50条分页33页全部返回；17组主题全部有内容且筛选分页完整；其中耐力与体适能35条，JSON编码通过。
- 原始`domain_code`、正文、内容形式、质量复核标记继续保留在追溯层。知识中心侧栏统一展示新主题目录；确认分类改变展示和筛选层，未重写正文或旧领域字段。
- 生产数据未执行数据库迁移，旧分类可随分类快照撤回；全量分类均通过版本化快照提供。

浏览器已验证桌面1440px、手机390px布局，筛选数量、分页、引文详情、空结果及加载失败提示均通过；无页面脚本错误。生产接口已验证17组主题均返回全量结果。

## 本地复验

从项目根目录执行，直接使用已归档输入：

```bash
python3 scripts/assemble_knowledge_normalization.py
python3 -m unittest discover -s scripts -p knowledge_normalization_test.py
```

汇总工具仅关联已有结果并检查结构，不执行语义分类。来源提案或目录快照发生变化时拒绝覆盖旧汇总，需单独建立并审阅新快照。该检查基于本地快照，生产当前版本一致性仍待发布前核验。

## 当前阻塞与后续顺序

group-05（柔韧、耐力140条）与group-06（感觉统合、认知、发展196条）的归并子任务被自动审批拒绝，返回“Invalid prompt: your prompt was flagged as potentially violating our usage policy”，未提供具体条款。两组保留待处理状态。最新诊断中基础子任务成功，两项原始指令重试仍被拒，具体原因尚未定位。平台支持需先查询内部调用链，决定处理方式；此前直接断言“管理员解除”缺少依据。复现信息见`automation-blocker.md`。

解除阻塞后接续顺序：完成336条目录归并与感觉统合边界复核；形成完整可评审结果；验证页面及版本一致性；按既定发布流程处理生产更新。
