<view class="container">
  <view class="logo-section">
    <view class="logo">追光</view>
    <view class="app-name">追光小牛运动成长中心</view>
    <view class="app-desc">员工内网工作台</view>
  </view>

  <view class="login-form">
    <view class="form-card">
      <view class="form-title">员工登录</view>

      <view class="error-msg" wx:if="{{errorMsg}}">{{errorMsg}}</view>

      <view class="agreement-box">
        <checkbox-group bindchange="onAgreementChange">
          <label class="agreement-line">
            <checkbox value="agree" checked="{{agreed}}" color="#ff6b35" />
            <text>我已阅读并同意</text>
            <text class="agreement-link" catchtap="openServiceAgreement">《用户服务协议》</text>
            <text>和</text>
            <text class="agreement-link" catchtap="openPrivacyPolicy">《隐私政策》</text>
          </label>
        </checkbox-group>
        <view class="agreement-tip">登录将用于员工身份核验、工作通知、学习记录、工作量日报与内部管理。</view>
      </view>

      <view class="login-mode-tip">
        <text wx:if="{{isWecomRuntime}}">当前已识别企业微信环境，优先使用企业微信一键登录，首次也支持账号密码完成身份关联。</text>
        <text wx:else>先使用账号密码完成身份校验，登录后可一键绑定本人微信。</text>
      </view>

      <button wx:if="{{isWecomRuntime}}" class="btn-login" bindtap="doWecomLogin" disabled="{{loading}}">
        <text wx:if="{{loading}}" class="loading"></text>
        <text wx:else>企业微信一键登录</text>
      </button>

      <view wx:if="{{isWecomRuntime}}" class="hint">
        企业微信成员已完成映射后，可直接进入首页；首次未映射时，请使用下方账号密码完成绑定。
      </view>

      <view class="input-group">
        <label class="input-label">账号</label>
        <input
          class="input"
          type="text"
          placeholder="请输入用户名"
          value="{{username}}"
          bindinput="onUsernameInput"
          autocomplete="username"
        />
      </view>

      <view class="input-group">
        <label class="input-label">密码</label>
        <input
          class="input"
          type="password"
          placeholder="请输入密码"
          value="{{password}}"
          bindinput="onPasswordInput"
          autocomplete="current-password"
        />
      </view>

      <button class="btn-login" bindtap="doLogin" disabled="{{loading}}">
        <text wx:if="{{loading}}" class="loading"></text>
        <text wx:else>登录</text>
      </button>

      <view class="hint">
        <text wx:if="{{isWecomRuntime}}">账号密码登录可用于首次建立企业微信成员映射，也可作为管理员兜底入口。</text>
        <text wx:else>默认使用账号密码登录。绑定微信后，后续登录会更快。</text>
      </view>
    </view>
  </view>
</view>
