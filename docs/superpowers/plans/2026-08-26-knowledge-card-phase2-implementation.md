# 1417 张教练与训练知识卡二期实施计划

- [x] 1. 建立生产基线核验与二期源数据契约
  - 对 `knowledge_categories`、`knowledge_items`、`user_knowledge_progress`、`drill_templates` 执行只读 schema/索引/关联核验脚本，输出可复现基线报告。
  - 为正式源目录建立只读 Markdown 清单，排除 README、备份目录和非 Markdown 文件。
  - 定义 7 种 `content_type`、8 大 `domain_code`、风险枚举、发布状态和缺失值规范；把 `status: 已审核`映射为内部整理状态，不映射为专业审核通过。
  - 参考设计规范第 3、6 节；不修改生产数据库。
  - [x] 1.1 编写源目录属性测试：文件名/card_id、frontmatter 必填字段、UTF-8、ID 唯一性和类型分布。
  - [x] 1.2 编写质量报告测试：风险计数、占位 URL、来源路径、正文哈希和统计汇总可重复。

- [x] 2. 实现确定性 Markdown 解析器与隔离数据包
  - 新建二期解析脚本，将每个 Markdown 映射为一个知识项，不拆成销售一期 K/S/D/C。
  - 保留原始 frontmatter、正文、来源文章、来源图片、源路径、源 SHA-256、规范化正文哈希和解析器版本。
  - 统一“原文未说明”、数组、URL 占位和本地路径的结构化表达；不伪造缺失信息。
  - 输出稳定排序、稳定 JSON 编码、批次统计、7 类/8 领域/风险分布和旧 194 条候选匹配报告。
  - 明确候选关系只报告，不自动合并、不自动覆盖旧知识。
  - [x] 2.1 编写字节级确定性测试：相同源目录重复生成结果和 SHA-256 完全一致。
  - [x] 2.2 编写解析属性测试：所有有效卡均有稳定 source_card_id、合法 content_type、合法 publication_status 和可追溯 source_sha256。
  - [x] 2.3 编写异常批次测试：缺 frontmatter、重复 ID、非法 URL、越界图片路径、无正文和未知卡型必须整批失败且不发布部分文件。

- [x] 3. 设计并创建知识库元数据、版本与个人功能 schema
  - 新增 `knowledge_import_batches`、`knowledge_item_sources`、`knowledge_item_versions`、`knowledge_item_relations`、`knowledge_favorites`、`knowledge_recent_views`、`knowledge_audit_logs` 建表迁移。
  - 为所有稳定编码、批次、版本、关系和用户资源增加唯一约束、必要索引和外键/孤儿保护。
  - 在允许的情况下最小扩展 `knowledge_items`：`item_code`、`content_type`、`domain_code`、`risk_level`、`publication_status`、`source_batch_id`、`current_version_id`；若生产基线禁止扩展，提供旁路元数据读取适配器。
  - 保持旧 `knowledge_items.id`、`user_knowledge_progress` 和 `drill_templates.knowledge_card_id` 语义不变。
  - 提供可重复执行、可验证的 schema 迁移和回滚脚本，禁止硬编码数据库连接。
  - [x] 3.1 编写 schema 验收测试：字段类型、JSON、唯一索引、外键/孤儿约束和 InnoDB 要求。
  - [x] 3.2 编写版本/关系属性测试：旧 ID 不变、同一 source_card_id 每批至多一条映射、版本号单调且历史版本不可覆盖。

- [x] 4. 实现安全导入、幂等、备份与精确回滚 CLI
  - 复用销售一期 CLI 的安全边界，但单独定义知识卡数据包 schema、批次锁和目标表集合。
  - 默认 dry-run 只读；计算新增、跳过、内容差异、候选关系和人工处理项；隔离数据不进入员工可见发布面。
  - apply 使用 `getDB()`、命名锁、导入前备份、事务、非目标行摘要断言、pending/completed manifest 和提交后校验。
  - 以 `item_code + source_sha256/normalized_hash` 实现幂等；已有旧 ID 只能更新版本/映射，不得删除重插。
  - 回滚只信任已完成或明确可恢复的 manifest，精确核验批次 ID/编码、未知关联、用户收藏/浏览和历史进度后再删除新增数据；存在风险时自动阻断。
  - 报告、备份和 manifest 必须位于 Web 根目录外，原子写入、0600、禁止覆盖已有文件。
  - [x] 4.1 编写 dry-run 调用图测试：无锁、无备份、无 manifest、无 INSERT/UPDATE/DELETE；显式 report 仅写用户请求文件。
  - [x] 4.2 编写幂等/冲突测试：重复执行 skip 全量、哈希变化进入 update/manual review、旧 ID 和关联保持不变。
  - [x] 4.3 编写故障注入测试：事务回滚、pending manifest 恢复、completed 写失败、锁释放、报告写失败和精确 rollback。

- [x] 5. 修复知识列表、分类、详情和搜索的认证授权边界
  - 统一要求已登录身份；移除客户端传入 role/stage 对服务端权限判断的覆盖。
  - 列表、分类计数、详情、搜索和旧分类入口统一只返回 `status=1` 且 `publication_status=published` 的内容。
  - 详情的相关知识、演练模板和关联资源必须再次执行同一可见性校验，阻断通过相关列表泄露受限内容。
  - 保留旧响应 envelope、筛选参数和深链兼容；补充 content_type/domain_code/risk_level 等新筛选但不破坏旧参数。
  - [x] 5.1 编写权限矩阵测试：匿名拒绝、已登录员工可读、隔离项不可读、发布项全员可读、客户端伪造 role/stage 无效。
  - [x] 5.2 编写 IDOR 回归测试：详情、相关知识、分类计数、演练关联和搜索结果均不能越权。

- [x] 6. 关闭全知识库完成/积分写入并实现收藏与最近浏览
  - 将 `progress.php` 的 GET 保持历史兼容读取；POST complete/update 明确拒绝写入，不调用积分奖励。
  - 确保月度统计继续读取历史表但不会因新浏览产生完成记录。
  - 实现登录、发布状态和资源存在性校验后的收藏/取消收藏 API。
  - 实现最近浏览的幂等 upsert、次数/时间更新、分页查询和下架后的历史保留策略。
  - [x] 6.1 编写进度测试：历史 GET 可读、POST 不改行数/更新时间/积分，非法 knowledge_id 不产生脏数据。
  - [x] 6.2 编写收藏/最近浏览属性测试：唯一约束、匿名拒绝、隔离项拒绝、重复操作幂等和下架历史保留。

- [x] 7. 实现受控 Markdown、图片和来源展示
  - 复用安全文本格式化原则，将标题、列表、表格、引用、链接和图片转换为固定安全 HTML；小程序统一使用安全 Markdown 渲染器。
  - 禁止原始 HTML、脚本、事件属性、危险协议、任意本地路径和未授权外链图片。
  - 图片仅允许 `/uploads/knowledge/` 受控资源路径；外链或危险协议图片降级为来源说明，后端同步使用本站 HTTPS/固定目录策略。
  - 后台与员工端使用同一内容安全契约，避免后台可编辑内容绕过员工端过滤；原始 HTML、raw Markdown 和本地 source_path 不作为员工端渲染输入。
  - [x] 7.1 编写 XSS/URL/路径安全测试：正文、来源、标题、图片 alt、表格和链接全部覆盖恶意输入。
  - [x] 7.2 编写渲染兼容测试：正常 Markdown、畸形表格、嵌套列表、中文标点和缺失媒体稳定降级。

- [x] 8. 实现内容运营后台与显式能力控制
  - 提供批次/质量报告查看、来源和风险查看、旧卡候选确认、合并/并存/拒绝关系管理。
  - 提供版本编辑、差异查看、发布、下架、指定版本回滚和审计日志查询。
  - 实现 `knowledge_view/edit/publish/rollback/audit` 能力；CEO 与指定教学主管拥有编辑/发布/回滚，其他管理者只读。
  - 后台编辑只创建新版本；合并保留旧知识 ID、历史进度和演练关联；操作日志不可由普通操作删除。
  - [x] 8.1 编写后台能力矩阵测试：CEO、指定教学主管、普通 manager、其他角色和匿名身份逐项验证。
  - [x] 8.2 编写版本/发布状态测试：隔离不可见、发布可见、下架不可见、回滚不改旧 ID/进度/演练关联。

- [x] 9. 改造员工端知识库为全量检索阅读
  - 保留现有列表、详情、搜索和深链入口，增加类型、专业领域、年龄、科目、训练类别、难度和风险筛选。
  - 删除完成学习、完成率、知识积分相关 UI；保留历史数据只读语义。
  - 增加收藏状态、收藏列表入口和最近浏览入口。
  - 展示来源、版本时间、风险和内容类型；支持受控正文、表格、链接和图片。
  - [x] 9.1 编写页面契约测试：旧 URL/参数/响应 envelope 兼容，新筛选和发布状态生效。
  - [x] 9.2 编写员工端安全渲染测试：恶意正文、来源和媒体不执行，收藏/最近浏览状态正确。

- [x] 10. 阶段检查点：隔离摄取验收
  - 确保解析、schema、导入器、权限和渲染测试全部通过，如有疑问请询问用户。
  - 生产只读 dry-run 必须确认 1417 张统计、旧194条候选、无冲突、无孤儿、无非目标更新。
  - 用户明确授权前禁止执行阶段 1 生产 `--apply`。
  - 2026-08-26 已完成：本地 44 项知识卡专项测试 41 通过、3 环境性跳过；生产已授权执行二期 schema 定向迁移，dry-run 确认 1417 条待插入、0 更新、0 manual review、5 条候选关系、核心旧数据计数未变，详见 `docs/ops/2026-08-26-knowledge-card-phase2-task10-local-gate.md`。

- [ ] 11. 阶段检查点：后台与人工合并验收
  - 确保候选关系、版本编辑、发布/下架/回滚、审计和能力矩阵测试全部通过，如有疑问请询问用户。
  - 确认旧 ID、历史进度、演练关联、既有销售培训卡和旧入口均未被破坏。
  - 用户明确确认人工合并结果后，才允许生产发布操作。

- [ ] 12. 阶段检查点：员工端全量发布验收
  - 确保列表、详情、搜索、筛选、收藏、最近浏览和受控渲染测试全部通过，如有疑问请询问用户。
  - 生产发布前后核验数据库计数、发布状态、来源映射、审计日志和无积分写入。
  - 发布后再次 dry-run 必须得到 insert/update=0；只执行 rollback dry-run，不主动执行 rollback apply。
